export type users = {
  id: number;
  name: string;
  points: number;
  history: string;
  politics: string;
  sports: string;
  media: string;
  science: string;
};

export type name = {
  id: string;
  name: string;
  difficulty: string;
};
